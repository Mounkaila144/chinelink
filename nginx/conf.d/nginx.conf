server {
    listen 80;
    server_name chine.nigerdev.com;

    root /var/www/public;
    index index.php;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass app:9000;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location ~ /\.ht {
        deny all;
    }

    location ~ /\.(env|config.php|database.php|artisan)$ {
        deny all;
    }

    location ~* \.(css|js|jpg|jpeg|gif|png|ico|svg|tif|tiff|woff|woff2|font|otf|ttf|eot)$ {
        expires 30d;
        access_log off;
        add_header Cache-Control "public";
    }

    location ~ /.well-known/acme-challenge {
        allow all;
        root /var/www/html;
    }
}

server {
    listen 443 ssl;
    server_name chine.nigerdev.com;

    ssl_certificate /etc/letsencrypt/live/chine.nigerdev.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/chine.nigerdev.com/privkey.pem;

    root /var/www/public;
    index index.php;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass app:9000;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location ~ /\.ht {
        deny all;
    }

    location ~ /\.(env|config.php|database.php|artisan)$ {
        deny all;
    }

    location ~* \.(css|js|jpg|jpeg|gif|png|ico|svg|tif|tiff|woff|woff2|font|otf|ttf|eot)$ {
        expires 30d;
        access_log off;
        add_header Cache-Control "public";
    }

    location ~ /.well-known/acme-challenge {
        allow all;
        root /var/www/html;
    }
}
