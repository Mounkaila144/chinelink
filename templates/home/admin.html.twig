{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-sm-12">
                <h2>Administrateur</h2>
                <div class="btn-group" role="group" aria-label="Basic example">
                    <a href="{{ path('app_produit_index')}}" class="btn btn-primary">Produit</a>
                    <a href="{{ path('app_categorie_index')}}" class="btn btn-danger">Catégorie</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
